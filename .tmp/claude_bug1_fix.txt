Hi Claude,

Please address the following bug:

**Symptom:** On the `PM-DECISION-CHECK` space, the dice roll button incorrectly reads "Roll for Bonus Cards" and does nothing when clicked.

**Required Fixes:**

1.  **Update `formatDiceRollButton`:** In `src/utils/buttonFormatting.ts`, modify the `formatDiceRollButton` function. It needs to inspect the `SpaceEffect` to determine the specific `card_type` (e.g., 'L' for `l_card`) and generate a more descriptive title, such as "Roll for Life Card".
2.  **Implement Dice Roll Logic:** In the appropriate service (`TurnService` or `DiceService`), add the logic to handle the dice roll for the `PM-DECISION-CHECK` space. This should perform the roll and correctly apply the "Draw 1 `l_card` if you roll a 1" effect.