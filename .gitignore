# Dependencies
/node_modules

# Build artifacts
/build
/dist
/public

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Test reports
/coverage

# Vite
.vite

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs and editors
.vscode/
.idea/

# OS-generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Developer scripts
# *.py
# *.js

# AI-AI Communication Infrastructure (Three-Directory JSON System)
# Message queues and runtime files (don't commit conversations)
/.server/claude-outbox/*.json
/.server/gemini-outbox/*.json
/.server/*.pid
/.server/*/.processing/
/.server/*/.unread/
/.server/*/.read/
/.server/*/.malformed/
/.server/*.log

# Keep these for version control:
# - mcp_client.py (Claude's inbox processor)
# - mcp_client_gemini.py (Gemini's inbox processor)
# - send_to_gemini.py (Claude's message sender)
# - send_to_claude.py (Gemini's message sender)
# - ai-collab.sh (Claude's client management)
# - ai-collab-gemini.sh (Gemini's client management)
# - start.sh (unified client management)
# - claude-mail-utils.sh (Gemini's mail utilities)
# - .server/*.md (documentation)
# - .server/ directory structure (empty directories)
